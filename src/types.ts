export interface Project {
  title: string;
  description: string;
  img: string;
  link: string;
}

export interface Experience {
  icon: React.ReactNode;
  title: string;
  company: string;
  dates: string;
}